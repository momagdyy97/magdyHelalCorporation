# VPS Docker behind nginx at /opt/magdyHelalCorporation.
#
# Binds Apache only on loopback: 127.0.0.1:8088 → container :80
# Public URL is WP_HOME from .env (default https://helal.co).
#
#   cp .env.example .env   # fill password placeholders
#   docker compose -f docker-compose.server.yml up -d
#
# Put nginx in front (X-Forwarded-Proto https) and issue a cert with
# certbot -d helal.co -d www.helal.co. Canonical URL is https://helal.co. See HOSTING.md.

services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-magdi_hilal}
      MYSQL_USER: ${MYSQL_USER:-magdi}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-changeme}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme_root}
    volumes:
      - db_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u${MYSQL_USER:-magdi}", "-p${MYSQL_PASSWORD:-changeme}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  wordpress:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${WP_PORT:-8088}:80"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${MYSQL_USER:-magdi}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD:-changeme}
      WORDPRESS_DB_NAME: ${MYSQL_DATABASE:-magdi_hilal}
      WORDPRESS_TABLE_PREFIX: mha_
      MHA_SYNC_THEME: "0"
      MHA_OPENAI_API_KEY: ${MHA_OPENAI_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      WP_HOME: ${WP_HOME:-https://helal.co}
      WP_SITEURL: ${WP_SITEURL:-https://helal.co}
      WP_URL: ${WP_URL:-https://helal.co}
      WP_ADMIN_EMAIL: ${WP_ADMIN_EMAIL:-info@helal.co}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_REDIS_HOST', 'redis');
        define('WP_REDIS_PORT', 6379);
        define('WP_REDIS_PREFIX', 'mha:');
        define('WP_HOME', '${WP_HOME:-https://helal.co}');
        define('WP_SITEURL', '${WP_SITEURL:-https://helal.co}');
        if (!empty($$_SERVER['HTTP_X_FORWARDED_PROTO']) && strpos($$_SERVER['HTTP_X_FORWARDED_PROTO'], 'https') !== false) {
            $$_SERVER['HTTPS'] = 'on';
        }
        define('FS_METHOD', 'direct');
        define('MYSQL_CLIENT_FLAGS', 0);
    volumes:
      - wp_data:/var/www/html
      - ./wp-content/themes/magdi-hilal-adco:/var/www/html/wp-content/themes/magdi-hilal-adco
      - ./wp-content/mu-plugins:/var/www/html/wp-content/mu-plugins
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  wpcli:
    image: wordpress:cli-php8.2
    volumes:
      - wp_data:/var/www/html
      - ./wp-content/themes/magdi-hilal-adco:/var/www/html/wp-content/themes/magdi-hilal-adco
      - ./wp-content/mu-plugins:/var/www/html/wp-content/mu-plugins
      - ./scripts:/scripts
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${MYSQL_USER:-magdi}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD:-changeme}
      WORDPRESS_DB_NAME: ${MYSQL_DATABASE:-magdi_hilal}
      WORDPRESS_TABLE_PREFIX: mha_
      WP_URL: ${WP_URL:-https://helal.co}
      WP_HOME: ${WP_HOME:-https://helal.co}
      WP_SITEURL: ${WP_SITEURL:-https://helal.co}
      WP_ADMIN_USER: ${WP_ADMIN_USER:-admin}
      WP_ADMIN_PASSWORD: ${WP_ADMIN_PASSWORD:-changeme}
      WP_ADMIN_EMAIL: ${WP_ADMIN_EMAIL:-info@helal.co}
    depends_on:
      - wordpress
      - db
    user: "33:33"
    profiles:
      - tools

volumes:
  db_data:
  redis_data:
  wp_data:
